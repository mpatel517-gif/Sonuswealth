# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test coverage output (regenerable via npm run test:coverage)
coverage/

# Windows null device artifact
nul

# Environment files (API keys, secrets)
.env
.env.*
*.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vs/

# Graphify cache (knowledge graph output, regenerable)
src/graphify-out/

# Local-only working dirs
.claude/
backups/

# Phase 2/3 additions
tests/reports/audit-*.md
tests/harness/_probe*.mjs
deepseek-output.txt

.superpowers/

# Scripts with hardcoded API keys — MUST refactor to env var + rotate key
# before these are committed. See 0-Active/audit/SESSION-CLOSE-2026-05-28.
deepseek-direct.mjs
scripts/deepseek-direct.mjs

# Local working dirs (handover archives, ad-hoc audit scratch)
.audit/
handover/

# Tax-bundle sync run artifacts (generated by scripts/sync-tax-bundle.mjs)
tests/.tax-sync-report.json
tests/.tax-sync-report.md
