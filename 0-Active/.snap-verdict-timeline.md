# Timeline flexibility-hardening — snap verdict

**Date:** 2026-06-08
**Agent:** Timeline BUILD agent (hardening pass, not rebuild)
**Branch:** claude/ecstatic-curie-dj2UH
**File touched:** `src/screens/Timeline.jsx` only
**Verdict:** BUILD-READY

## Verification method
- Dev server `caelixa-dev` (cd's into Desktop/finio), serverId `557dca12-ea36-4709-83d4-10fb09aff482` (restarted fresh after edits to clear stale HMR state).
- Both personas loaded: Bruce `?demo=a&tab=timeline`, Mr T `?demo=mrt-core&tab=timeline`.
- `main` scrolled internally (`main.scrollTop`) to reach §A–§F; scraped `main.innerText` + DOM range inputs.
- Each new control drag-tested via native setter (`HTMLInputElement.prototype.value` set + input/change events), scraped scoped to the control's card before→after.
- Console checked with `preview_console_logs(level:'error')` after every change and after every drag batch.
- Screenshots captured (desktop dark, mobile light) — Timeline screenshots worked after restart.

## Console error-tail (Supabase-only — required gate)
```
[error] Missing Supabase environment variables. Check .env.local   (×N)
```
No "Maximum update depth exceeded". No Vite reload failures on the fresh server. NO render loop. (The prior agent's crash was an `entity`-in-setState-effect loop — avoided: all new state is seeded via lazy `useState` initialisers or derived via pure `useMemo`; `entity` never enters a setState path.)

## Scope items (all 5 done)
1. **Goal-seek target → grabbable ScrubTrack** — typed `<input>` replaced with ported ScrubTrack (drag + keyboard range mirror). Drag re-solves `goalSeek()` live. Range is metric-aware (scores 0–100; netWorth £0–2×NW; iht £0–…).
2. **Funded headline fixed** — was "Awaiting data · Funded —". Now falls back to canonical `fundedRatio(entity).ratio` for retirement/cashflow plans when saved scenarios lack `deltaResults.fundedRatio`. Provenance line states the real source + confidence.
3. **PRC/PCC "Coming next" leak removed** — dead sub-anchor strip deleted (PP-9 internal-code-on-surface + §9 dead-affordance).
4. **§A retirement-age override (spec §4.7 A.5)** — grabbable ScrubTrack drives the modelled stage name, "enter Decumulation at N" line, years-to-retirement, and a live ↧marker on the 7-stage strip. NMPA warning when below NMPA.
5. **§C gift amount/date adjustable** — gift-clock entry re-driven by two ScrubTracks (amount + years-elapsed). Statutory entries (ISA/SA/SIPP-IHT) stay fixed.

Charts (item 4 tail): added a "today" divider + present-point marker to the §B score-history sparklines. NOTE: there is NO £-over-time projection line chart on Timeline (§B = score history; §F = milestone list), so there is no £-band to add here — flagged below, not fabricated.

## Drag-test before→after (scoped to each control's card)
| Control | Engine fn driven | Before | After |
|---|---|---|---|
| §A retirement age (Bruce) | pure stage derivation + strip marker | "Decumulation at age 66 · 4 years away", marker ↧66 | drag 72 → "Decumulation at 72 · 10 years away", marker ↧72 (left 62.07%); drag 78 → "Preservation at 78" |
| §C gift years (Bruce) | `giftPct` / `taperBand` | "33% elapsed · Full IHT · 40%" | drag 6.0 → "86% elapsed · Taper 20% · 8%" |
| §C gift amount (Bruce) | display | "£500k gifted" | drag 200k → "£200k gifted" |
| §C gift years (Mr T) | `giftPct` / `taperBand` | "49% · Taper 80% · 32%" | drag 7.0 → "100% · IHT-free · 0%" |
| Goal-seek target (Bruce, wealthScore) | `goalSeek()` | no paths | drag 95 → paths re-ranked: drawdown achieves 100, isa-fund/gift achieve 66, protection-add 54 |

rangeControls: 0 → 3 on-screen (retirement age, gift amount, gift years) + 1 in goal-seek sheet = 4 total.

## Tie-outs (both personas)
| Surface | Displayed | Engine | Match |
|---|---|---|---|
| Bruce funded headline | 47% · MEDIUM | `fundedRatio(persona-a).ratio = 0.47`, confidence MEDIUM | ✅ |
| Mr T funded headline | 122% · saved scenario | `deltaResults.fundedRatio = 1.22` (scenario beats engine fallback — correct precedence) | ✅ |
| Bruce §A modelled stage | Decumulation @66 / Preservation @78 | STAGES boundary map | ✅ |
| Gift taper | rate per band | `taperBand(date).rate` | ✅ |
| CoI chips | unchanged | `costOfInaction(entity).byAction` (untouched — read-only from T&E, kept) | ✅ |

## Build / tests
- `npm run build` — ✓ 374 modules, exit 0
- `npm run regression:check` — **180/180 matched · 0 drift** (overrides are display-only; engine outputs unchanged)
- `test:l3-8-goal-seek` 27/27 · `test:goal-engine` 38/38 · `test:scenario-engine` 31/31 · `test:tax-guards` PASS (0 violations)

## Screenshot status
Captured: desktop dark (funded headline 47% off-track bar; PRC strip gone), §A scrolled (↧66 strip marker + retirement-age ScrubTrack + "Modelled…" line + PENDING SIGN-OFF disclaimer + DOB causality stripe), mobile light (§A controls reachable, no overflow). All render cleanly.

## Flags for orchestrator / external calc reviewer
- All engine outputs surfaced are labelled "PENDING PROFESSIONAL SIGN-OFF" (funded %, retirement-age model, gift taper). Not certified.
- **No £-over-time projection line chart exists on Timeline** — item-4's "labelled band + today divider" requirement only applies to a £-projection chart, which lives on MyMoney/Cashflow, not here. Added the today-divider to the score-history sparklines instead; did NOT fabricate a new NW projection chart (would be scope-creep + entity-ref-loop risk). Orchestrator decision if a NW band-chart is wanted on Timeline.
- App theme tokens don't fully follow `prefers-color-scheme` in the MCP light emulation (pre-existing, not introduced here).
