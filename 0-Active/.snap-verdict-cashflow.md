# Cashflow founder-review hardening — snap verdict (2026-06-08)

Branch `claude/ecstatic-curie-dj2UH`. Screen `src/screens/Cashflow.jsx`. Personas: Bruce `?demo=a&tab=flow` (decumulator), Mr T `?demo=mrt-core&tab=flow` (accumulator, the reviewed one). Verified LIGHT (founder review) + dark, mobile + desktop + tablet.

## Canonical tie-out source (in-page engine scrape, both personas)
`cashflowFlow(entity)` — the ONE source. Every net figure derives from it.

| | gross | taxAndNI | committed | essentials | debt | protection | surplusAnnual | surplusMonthly |
|---|---|---|---|---|---|---|---|---|
| Bruce | 47,173 | 5,387 | 0 | 28,304 | 13,200 | 4,980 | −4,698 | **−391** |
| Mr T | 79,110 | 16,599 | 10,500 | 47,466 | 14,268 | 1,608 | −11,331 | **−944** |

## Per-point

### C-1 — Choices toggle moved inline next to "Money sections"
- Before: Choices floated centred inside `X28TopBar` (own row, looked bare).
- After: one flex row — `<MoneyXDrawer variant="compact">` ("Money sections ▾") + a sibling Choices toggle button (`Cashflow.jsx` body ~1688). `X28TopBar` removed (it only wrapped that one button); unused import dropped.
- Proof: DOM — both buttons `sameRow:true`, Money left=144 / Choices left=271; clicking Choices opens "CHOICES YOU CAN MAKE FROM CASHFLOW" chip panel. Snapped light+dark, mobile+desktop+tablet.

### C-2 — the two info lenses are now drillable
- `PurposeStatement` hero lenses ("On track / 27% to FI" + "Resilience / cash buffer 7.2 months" for accumulators; "Your plan" + "If markets disappoint" for decumulators) were info-only `<div>`s. Now each is a button with "Dig in ›" → `HeroLensDrill` overlay: **the number → what makes it up (drivers) → how it's worked out (the rule)** + PENDING PROFESSIONAL SIGN-OFF.
- Proof (Mr T): "On track" drill → Progress 27% · wealth 6.7× · FI target £1.50m + methodology. "Resilience" drill → 7.2 months · accessible cash £29k · monthly essential £4k · **monthly surplus −£944/mo** (canonical). (Bruce): "Your plan" drill → target £96k = secure £31k + pots £65k (ties out).

### C-3 — back-to-source route on every drill another tab owns
- Hero-lens drills (FI/buffer/plan read assets on My Money) → "Full detail: your assets on My Money →" routing `onNav('money')`.
- Savings drill → "Full detail: your pensions & ISAs on My Money →".
- Sankey `MoneyFlowDrill` node labels made real nav links where another tab owns the detail: income/property/liabilities/pension → `onNav('money')`; tax/protection → `onNav('tax')`. Essentials ("spending detail") deliberately has NO link — Cashflow owns it (per C-3 "don't add where Cashflow is the owner").
- `onNav` threaded: Cashflow → NowDrawer / money-map → CashflowMoneySankey → MoneyFlowDrill; and → PurposeStatement.

### C-4 — tiles
- **(a) New tiles added** (each: real selector from canonical `flow`, Dig-in drill, FCA-safe, PENDING SIGN-OFF):
  - **"How fast am I building wealth?" (savings rate)** — `committed/gross`. Mr T = **13.3% saved · £875/mo**. Source: founder C-4a "Savings rate / wealth-building velocity" + spec §3B `cashflowHealth.components.surplusRatio`. Directly reframes C-5's £875/mo "gap" as a savings RATE, not a shortfall. Drill: rate · what's-going-to-wealth tie-out (£10,500/yr = 13.3% of £79,110) · 15%-rule-of-thumb benchmark marker · methodology.
  - **"Where does my money go?" (spending breakdown)** — essentials/debt/protection/tax/saving composition. Mr T = **60% essentials** (47,466/79,110). Source: founder C-4a "Where my money goes" + spec §4.4/§4.5. Promotes the buried Now-drawer mini-bar to a full surface.
- **(b) Re-sequenced** by decision importance: `now(0) → savings(1) → drawdown(2) → lastability(3) → spending(4) → methods(5) → costs(6) → resilience(7) → whatif(8)` (`TILE_ORDER`). Verified live order on both personas.
- **(c) Sparklines** — kept the real-or-nothing rule: only `lastability` (pot trajectory) and `resilience` (MC median) have honest single series, so only those carry sparklines. Savings/spending have NO real time series (no contribution/spend history) → omitted honestly per founder "where none exists, omit". Did NOT fabricate trends.
- **(d) "Dig in ›" standardised across screens.** `QuestionTile` fallback `'View ›'`→`'Dig in ›'`. Cross-screen renames: `CategoryTile.jsx`, `LiabilityTile.jsx` ("View detail →"→"Dig in ›"), `BusinessDrillDown.jsx` (3×), `PropertyDrillDown.jsx` (2×) ("View details ›"→"Dig in ›"). Live proof: MyMoney now shows 6 "Dig in", **0 "View detail/details"**. (Remaining "View detail" hits are code comments only.)

### C-5 — numbers reconcile (the headline fix)
Root cause confirmed: surfaces masked/recomputed net independently. Fixes (all derive from `cashflowFlow`):
- **"Where your money flows" stat** (`Cashflow.jsx:1320`): was `${_gk(income)} in · ${_gk(out)} out` — `_gk` rounds to nearest £1k so £6,593 in / £7,537 out BOTH showed "£7k", masking the deficit; AND `out` omitted protection (£134/mo for Mr T → −£810 vs canonical −£944). Now: `${_gx(income)} in · ${_gx(out)} out · −£Z net` with exact `_gx` formatter, and `out = income − msNet` (canonical, protection included).
- **Two NAMED lenses** added to the Now lead, derived ONCE: **Gross monthly gap** = `−surplusMonthly` (ONE value); **True shortfall** = gap − deliberate saving (`committed/12`). Mr T = **−£944 gross gap / −£69 true shortfall** (after £875/mo saving). The £944-vs-£894 (£50) and the phantom £19 are dead — both were the old protection-omitting / mis-rounded local recomputes.
- Lead sentence + in/out strip → exact `_gx` (no more "£7k comes in / £7k goes out").
- Fine-tune slider drives off canonical net: set Extra income £900 → "New monthly position" −£944 → **−£44** (−944+900). Tied out.

#### C-5 tie-out table — every surface == cashflowFlow
| Surface | Bruce | Mr T | == canonical? |
|---|---|---|---|
| `cashflowFlow.surplusMonthly` (canonical) | −£391 | −£944 | — |
| Now hero "In deficit £X/mo" | £391 | £944 | ✓ |
| Now "in/out/net" stat | (n/a, committed 0) | £6,593 in · £7,537 out · −£944 net | ✓ |
| Now lead sentence | −£391 | £6,593 in / £7,537 out / draw £944 | ✓ |
| Gross monthly gap lens | — (no split, committed 0) | −£944 | ✓ |
| True shortfall lens | — | −£69 (944−875) | ✓ |
| "Ways to close the gap" header | £391 | £944 | ✓ |
| Sankey / waterfall "net deficit/yr" | −£5k (−4,698) | −£11k (−11,331) = surplusAnnual | ✓ |
| Resilience-lens drill "monthly surplus" | — | −£944 | ✓ |
| Savings tile / drill | 0% (committed 0) | 13.3% · £10,500/yr · £875/mo | ✓ |
| Spending tile / drill | 60% ess | 60% ess (47,466/79,110) | ✓ |
| Hero-plan drill (decum) | £96k=£31k+£65k | — | ✓ |

## Files touched
- `src/screens/Cashflow.jsx` — all of C-1, C-2, C-3, C-4(a/b/c), C-5; `_gx` exact formatter; X28TopBar import removed.
- Cross-screen Dig-in rename (C-4d) — `src/components/MyMoney/CategoryTile.jsx`, `LiabilityTile.jsx`, `BusinessDrillDown.jsx`, `PropertyDrillDown.jsx`.

## Verify
- `npm run build` → ✓ 374 modules, exit 0.
- `npm run regression:check` → **180/180 matched, 0 drift** (engine untouched — display-only changes reading canonical engine).
- `npm run test:decumulation` → 26/26 pass.
- Console (both personas, Cashflow + MyMoney): only `Missing Supabase environment variables` (expected, allowed). **No "Maximum update depth" / render loop.**
- Screenshots: Mr T mobile light + desktop light + Now-drawer light; Bruce tablet dark. Light confirmed (founder reviews in light; app theme = `data-theme`/`sw_theme`, NOT prefers-color-scheme — set to light explicitly).
- `preview_screenshot` wedged once at 30s → `preview_stop`+`preview_start` (fresh serverId) cleared it (per memory).

## Boundary
Engine arithmetic NOT certified — all derived display metrics labelled "PENDING PROFESSIONAL SIGN-OFF". No formulas invented; savings rate / spending split / lenses are pure ratios of existing canonical `cashflowFlow` fields. C-5 reconciliation is structural (single-source) and done.

## VERDICT: BUILD-READY
