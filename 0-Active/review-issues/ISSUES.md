# Live review — issue log

Drop screenshots into THIS folder (`0-Active/review-issues/`). Name them by screen + number so I can map them, e.g. `tax-01.png`, `risk-03.png`, `timeline-02.png`. Then list each below (screenshot optional — a clear text description is often enough).

I read the images directly off disk, so there's **no 5-per-conversation limit** — put as many as you want in the folder and I'll process them all in one pass.

Persona + screen for each issue helps a lot: which `?demo=` (a = Bruce, mrt-core = Mr T) and which tab.

---

## Format (copy a row per issue)

| # | Screen (tab) | Persona | Screenshot file | What's wrong | (optional) what it should be |
|---|---|---|---|---|---|
| 1 | Tax | a | tax-01.png | e.g. "IHT figure overlaps the label on mobile" | "should sit on its own line" |
| 2 | Risk | mrt-core | risk-01.png | | |
| 3 | Timeline | a | | "scroll feels trapped — can't reach §F" | |

---

## Issues

(add rows above, or just write them free-form here — screen + description is all I need)
