# Tax & Estate fix-pass — snap verdict (2026-06-07)

Branch `claude/ecstatic-curie-dj2UH`. Serving `caelixa-dev` port 5173.
Server restart: prior serverId `63955dfa…` not in preview_list; preview_start
gave fresh serverId `3a3fbfaa-7d5d-4b80-9c70-dc8186296b6d`.

## Verdict: BUILD-READY

`npm run build` exit 0 (373 modules). Tests: l3-2-iht 42/42, projection 24/24,
tax-guards PASS (0 new violations). All 5 fixes coded, DOM-verified, tie-outs green.

---

## Fix-by-fix

### #1 — stale statute (P1)
`TaxEstate.jsx:2567` "Finance Act 2024" → "Finance Act 2026". Grep of `src/`
confirmed this was the only SIPP-into-estate instance; the other 4 FA-2024
refs (NRB/RNRB freeze, LTA abolition, FIG/TRF) are legitimately FA 2024 — left.

### #2 — countdown reconciliation (P2)
Was 4 independent computations (tile ceil→303, IHTDeltaCard floor→302,
canonical-metrics round, banner round). Now ONE canonical helper
`sippIhtCountdownDays(nowMs)` in `fq-calculator.js` (floor, UTC pivot 2027-04-06).
- `daysLeft()` delegates to it (CoI daily-rate denominator matches UI).
- `TaxEstate.jsx` sub-anchor tile (`daysToPensionIHT`) → helper.
- `TaxEstate.jsx` `SippIhtCountdownBanner` → helper.
- `IHTDeltaCard.jsx` `daysToApril2027` → helper.
- `canonical-metrics.js:464` `daysUntilApril2027` → floor/UTC to match.
PROOF: every visible day-count = **302**, no `303` anywhere in DOM. IHTDeltaCard
a11y countdown, sub-anchor tile, and banner all read 302 for both personas.

### #3 — scoped CoI (FOUNDER RULING)
`EstateCoIOdometer` now renders the estate slice via
`costOfInaction(entity,'estatePlanning')` (= `totalCoI().byDomain.estatePlanning`),
relabelled "Cost of inaction · estate" + sub "Estate-planning actions only —
see Home for the all-domains figure." byAction sheet filtered to estate domains.
Added `label`/`scopeSub` props to `CoIOdometer.jsx`.
- Bruce estate odometer = **£70,000** (scraped) == engine costOfInaction(a,'estatePlanning')=70000
- Bruce header COI = **£427k** (scraped) == totalCoI=426,775 UNCHANGED
- Home CoI anchor = **£427k** (scraped on /?demo=a&tab=home) UNCHANGED — no cross-tab regression
- Mr T estate odometer = £0 (no estate-planning slice for a 35yo accumulator — plausible, renders gracefully)

**FLAG FOR CALC REVIEWER:** the prompt named `coiForDomain(entity,'estatePlanning')`,
but `canonical-metrics.coiForDomain` has NO 'estatePlanning' case and returns
**null** (string-builder for pensions/investments/property/business/protection/
liabilities/cash only). The numeric estate slice is
`costOfInaction(...,'estatePlanning')` (spec §Q1.2 canonical) = £70k. Used that.
The coiForDomain naming collision (numeric dispatcher in fq-calculator vs
string-builder in canonical-metrics, same name) is a latent trap.

### #4 — a11y (P3)
`SliderRow` (3 IHT-modeller sliders: SIPP drawdown / Gifts-PETs / BPR) gained
`aria-label` (label + live value + purpose) + `aria-valuetext`.

### #5 — flexibility scrub (P2, core item)
New shared `ScrubTrack` helper (relative-drag pattern from DecisionCharts
startScrub/BarGrip + keyboard `<input type=range>` mirror).
- **DrawdownMatrix**: added "Model a drawdown" driver. Drag/key → Tax/Net/IHT-saved
  rescale live (interpolated off engine matrix rows), 60%-band warning toggles,
  matrix row highlights to match. Driver range = engine matrix min..max.
- **RNRB gauge**: gross estate is now the draggable driver →
  `rnrbTaper(grossEstate)` recomputes effective RNRB / lost-to-taper /
  IHT-on-lost live. Taper threshold + RNRB cap read from `TAX` (not hardcoded).
  Reset-to-base button.

---

## Tie-out table (scraped vs engine)

| Surface | Bruce (a) scraped | Bruce engine | Mr T scraped |
|---|---|---|---|
| Header COI (all-domain) | £427k | totalCoI 426,775 | £97k |
| Estate CoI odometer | £70,000 | costOfInaction(estatePlanning)=70000 | £0 |
| Home CoI anchor | £427k | (unchanged) | — |
| Countdown days (tile) | 302 | sippIhtCountdownDays=302 | 302 |
| Countdown days (banner) | 302 | 302 | 302 |
| Countdown days (IHTDeltaCard a11y) | 302 | 302 | 302 |
| IHT today | £1,088,000 | — | £308,084 |
| IHT delta | +£340,000 | — | +£67,000 |
| IHT post-2027 (today+delta) | £1,428,000 ✓ | — | £375,084 ✓ |

Single countdown value proven: **302** across all surfaces, both personas. No 303.

## Scrub drag-test (Bruce, native-setter driven)

**Drawdown** (input min 0 max 100000): £100k → £85k
- Tax £32k → **£26k**, Net £108k → **£93k**, IHT saved £40k → **£34k**
- 60%-band warning present at £100k, gone at £85k (below £100k cliff)
All three dependent figures rescaled independently and live.

**RNRB** (gross-estate driver): £1.96m → £2.60m → £1.50m
- Lost-to-taper £0 → **£175,000** (taper warning banner appeared) → £0
- Effective RNRB + IHT-on-lost-band rescaled off rnrbTaper(grossEstate) live
- Reset button restored base

## Screenshots
- Bruce desktop dark — header COI £427k, SIPP banner "10 MONTHS LEFT / ~10 months" (302d)
- Mr T mobile dark canonical T&E — Tax3/Estate1 sub-tabs, sub-anchor
  "IHT TODAY £308k · FAMILY RECEIVES £962k · PENSION-IHT 302 days Until 6 Apr 2027",
  SIPP banner 10mo (clean canonical-screen shot)
NOTE: full 3×2 matrix not captured — the SPA route/persona state races on
eval-driven navigation + resize (resize remounts to default route; deep-link
`tab=tax` lands on the Tax&Allowances statements view with the canonical
TaxEstate sub-tabs nested below). All substantive verification done via DOM
scrape + native-setter drag, the load-bearing §9.5 Gate-2 evidence.

## Test results
- npm run build: exit 0, 373 modules
- test:l3-2-iht: 42/42
- test:projection: 24/24
- test:tax-guards: PASS (0 new key/literal violations)

## For external calc reviewer
1. `coiForDomain(entity,'estatePlanning')` (canonical-metrics) returns null — no
   estatePlanning case. Numeric estate CoI is `costOfInaction(...,'estatePlanning')`.
   Consolidate the two coiForDomain functions / add the missing case.
2. Estate CoI magnitudes (Bruce £70k, Mr T £0) and IHT figures labelled provisional
   — PENDING PROFESSIONAL SIGN-OFF. Structure/binding verified, arithmetic not certified.
3. DrawdownMatrix live readout is linear-interpolated between engine rows (labelled
   provisional in-UI) — confirm interpolation acceptable vs per-point engine recompute.
